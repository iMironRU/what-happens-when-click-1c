## Приложения

### Приложение A. Глоссарий терминов

Краткий справочник терминов, использованных в статье. В первую очередь — для тех, кто пришёл из других стеков и встретил незнакомое слово.

**1С:Предприятие** — режим работы платформы, в котором конечный пользователь работает с прикладным решением: вводит документы, формирует отчёты, ведёт учёт. Запускается из лаунчера кнопкой «1С:Предприятие». Технически — `1cv8.exe ENTERPRISE` или `1cv8c.exe` для тонкого клиента.

**1Cv8.1CD** — основной файл данных файловой информационной базы. Собственный формат 1С с страничной структурой. Внутри — и пользовательские данные, и код конфигурации.

**1Cv8.1CL** — служебный блокировочный файл, лежащий рядом с `1Cv8.1CD`. Координирует доступ нескольких клиентов к файловой базе.

**`1cestart.exe`** — лаунчер платформы 1С. Маленькая программа, единственная задача которой — выбрать правильную версию платформы и тип клиента, и запустить их. Лежит в `C:\Program Files\1cv8\common\`.

**`1cestart.cfg`** — конфигурационный файл лаунчера. Существует в нескольких экземплярах с разными приоритетами (пользовательский, общесистемный, корпоративный).

**`1cv8.exe`** — толстый клиент платформы 1С. Универсальный клиент, поддерживающий все типы форм и Конфигуратор. Запускается с разными ключами командной строки (`ENTERPRISE`, `CONFIG`).

**`1cv8c.exe`** — тонкий клиент платформы 1С. Облегчённый клиент, работающий только с управляемыми формами. Не поддерживает Конфигуратор.

**Аутентификация ОС** — механизм входа в 1С под текущим пользователем операционной системы, без ввода имени и пароля. Использует стандартный SSPI Windows (Kerberos или NTLM).

**Базовая (комплектация)** — самая ограниченная и дешёвая комплектация типовой конфигурации. Только файловая база, один пользователь, нельзя править конфигурацию.

**Веб-клиент** — версия тонкого клиента, реализованная в браузере на JavaScript/HTML. Отдаётся веб-публикацией. Не требует установки на машине пользователя.

**Внешнее соединение** — механизм программного подключения к информационной базе из других приложений (через COM-коннектор `V83.COMConnector`). В этой статье подробно не разбирался.

**Журнал регистрации** — внутренний журнал событий конкретной информационной базы. Хранится в служебных таблицах ИБ. Видны входы пользователей, изменения объектов, ошибки.

**Информационная база (ИБ)** — конкретный развёрнутый экземпляр прикладного решения вместе с накопленными данными. См. часть 0.1.

**Клиент-серверная база** — информационная база, обслуживаемая сервером 1С (`ragent`/`rmngr`/`rphost`) и хранящая данные в одной из поддерживаемых СУБД. Альтернатива — файловая база.

**Кластер 1С** — группа процессов сервера 1С (`ragent` + `rmngr` + один или несколько `rphost`), обслуживающих информационные базы. Может работать на одном хосте или быть распределённым по нескольким.

**Конфигурация** — прикладное решение как таковое: модель данных + код + формы + роли. См. часть 0.1.

**Конфигуратор** — режим работы платформы для разработчика и администратора. Запускается из лаунчера кнопкой «Конфигуратор». Технически — `1cv8.exe CONFIG`. В тонком клиенте недоступен.

**КОРП (комплектация)** — расширенная комплектация типовой конфигурации с дополнительными возможностями для крупных предприятий.

**Лаунчер** — он же `1cestart.exe`. См. выше.

**Метаданные** — машинное описание структуры конфигурации: какие есть объекты, реквизиты, формы, роли. Хранятся в служебных таблицах ИБ. См. часть 6.1.

**Модуль** — единица кода на встроенном языке 1С. Бывают модули объектов, модули форм, общие модули, модуль сеанса, модуль управляемого приложения. Каждый модуль компилируется в свой контекст (серверный/клиентский).

**Модуль сеанса** — специальный серверный модуль конфигурации, в котором живёт обработчик `УстановкаПараметровСеанса`. Компилируется на сервере при старте сеанса.

**Модуль управляемого приложения** — специальный клиентский модуль конфигурации, в котором живут обработчики `ПередНачаломРаботыСистемы`, `ПриНачалеРаботыСистемы` и другие глобальные клиентские обработчики.

**Параметры сеанса** — типизированные именованные значения, привязанные к конкретному сеансу пользователя и используемые прикладным кодом и шаблонами RLS. См. часть 5.3.

**Подсистема** — группировка прикладных объектов конфигурации по «смысловому назначению». Из подсистем строится панель разделов главного окна. См. часть 8.3.

**ПРОФ (комплектация)** — стандартная коммерческая комплектация типовой конфигурации. Можно работать в клиент-серверной схеме, можно нескольким пользователям, можно дорабатывать конфигурацию.

**`ragent`** — агент сервера 1С. Служба, управляющая остальными процессами кластера на конкретном хосте. По умолчанию слушает порт 1540.

**Расширение конфигурации** — отдельный объект, который подключается к основной конфигурации в момент работы и меняет её поведение, не трогая исходников. См. части 0.7 и 6.2.

**Регистр накопления** — один из типов прикладных объектов конфигурации. Предназначен для накопления количественных или суммовых остатков по измерениям. Документы делают «движения» в регистрах.

**Регистр сведений** — другой тип прикладных объектов. Предназначен для хранения характеристик, которые могут меняться во времени (курсы валют, цены, статусы).

**Реквизит** — поле объекта конфигурации (справочника, документа). Аналог «колонки» в обычной БД, но с типизацией и метаданными.

**Роль** — именованный набор разрешений в конфигурации. Пользователю назначается одна или несколько ролей, и его итоговые права — объединение прав всех ролей.

**`rmngr`** — менеджер кластера 1С. «Мозг» кластера, который знает про базы, рабочие процессы, сеансы, балансировку. По умолчанию слушает порт 1541.

**`rphost`** — рабочий процесс сервера 1С. Процесс, в котором реально исполняется прикладной код, формируются ответы на запросы клиентов, происходит обмен с СУБД. По умолчанию слушает порты из диапазона 1560–1591.

**RLS (Row-Level Security)** — механизм ограничения доступа к данным на уровне отдельных строк. В 1С реализован через шаблоны ограничений в ролях. См. часть 6.3.

**Сеанс** — активная сессия работы пользователя с информационной базой. Имеет уникальный номер, видна в консоли администрирования, занимает одну клиентскую лицензию.

**Сервер 1С** — служба, обслуживающая клиент-серверные информационные базы. Состоит из процессов `ragent`/`rmngr`/`rphost`. Не путать с сервером СУБД — это две разные сущности.

**Серверная лицензия** — лицензия на сам сервер 1С. Нужна для запуска кластера и обслуживания клиент-серверных баз. Существует независимо от клиентских лицензий.

**Серверный вызов** — обращение клиентского кода к серверу для выполнения серверной процедуры. В управляемых формах серверные вызовы стоит минимизировать, потому что они идут через сеть.

**Справочник** — один из типов прикладных объектов. Предназначен для хранения списков объектов с иерархией: контрагенты, номенклатура, сотрудники, и так далее.

**`srvinfo`** — служебный каталог агента сервера 1С, в котором хранится «база данных» кластера: список зарегистрированных баз, параметры подключения к СУБД, журналы регистрации, контексты сеансов.

**Тонкий клиент** — он же `1cv8c.exe`. Облегчённый клиент платформы. Работает только с управляемыми формами. Может подключаться напрямую к серверу 1С или через веб-публикацию.

**Толстый клиент** — он же `1cv8.exe` в режиме `ENTERPRISE`. Полнофункциональный клиент. Поддерживает обычные и управляемые формы. Запускается также в режиме Конфигуратора.

**Управляемая форма** — современная модель форм в 1С, описанная декларативно в метаданных и рендерящаяся каждым клиентом по-своему (нативный WinAPI, Qt, HTML/JS). См. часть 8.5.

**`УстановкаПараметровСеанса`** — обработчик в модуле сеанса, который вычисляет значения параметров сеанса при первом обращении к ним. См. часть 5.3.

**Файловая база** — информационная база, хранящаяся в виде каталога с файлом `1Cv8.1CD`. Не требует сервера 1С. Подходит для одиночной работы или малых установок.

**HASP-ключ** — USB-ключ защиты от компании Thales (бывший SafeNet/Aladdin), используемый для аппаратной лицензии 1С.

### Приложение B. Внешние ссылки для углубления

Здесь — короткий список внешних источников, по которым можно копать глубже отдельные темы статьи. Список не претендует на полноту: интернет про 1С большой, и у каждого опытного одинэсника свои любимые ресурсы. Это — отправные точки.

#### Официальные источники

- **ИТС (Информационно-технологическое сопровождение)** — официальная база знаний фирмы «1С». Документация по платформе, методические рекомендации, статьи. Часть материалов общедоступна, часть требует подписки. https://its.1c.ru
- **Документация платформы** — официальная документация на встроенный язык, объекты конфигурации, синтакс-помощник. Доступна как из самой платформы (через Конфигуратор), так и онлайн.
- **1С:Сообщество разработчиков** — официальный форум разработчиков 1С. https://1c.ru

#### Сообщество и блоги

- **Infostart** — крупнейшая русскоязычная площадка для одинэсников. Статьи, обработки, обсуждения. https://infostart.ru
- **Мисте** — другой популярный форум сообщества. https://www.forum.mista.ru

#### Источники, использованные при подготовке статьи

- **online-ufa.ru про формат `.v8i`** — описание формата файла информационных баз и его параметров: https://www.online-ufa.ru/content/articles/1c-v8i/
- **Infostart про процесс запуска 1С** — статья с разбором этапов запуска: https://infostart.ru/1c/articles/281119/

#### Про конкретные технологии, использованные в статье

- **`what-happens-when` от Алекса Гейнора** — оригинальная статья, послужившая вдохновением для этого формата: https://github.com/alex/what-happens-when
- **Документация PostgreSQL Pro** — про специальную сборку PostgreSQL под 1С: https://postgrespro.ru
- **Документация Sentinel HASP** — про систему лицензирования, используемую в HASP-ключах 1С: документация Thales

### Приложение C. Шпаргалка «куда смотреть, если не запускается»

Этот раздел — практический. Он не объясняет, **почему** что-то сломалось — для этого читайте основной текст. Он отвечает на вопрос «куда первым делом смотреть», когда конкретный пользователь говорит «у меня 1С не запускается».

#### Шаг 1: что именно видит пользователь?

Самый первый вопрос — **на каком этапе** запуск останавливается. Это сразу сужает зону поиска радикально.

- **Ничего не происходит вообще** при двойном клике, или Windows показывает сообщение про ярлык/программу → проблема на уровне ОС, до запуска лаунчера. См. часть I.
- **Открывается окно «Запуск 1С:Предприятия»**, но в нём что-то не так (пустой список баз, серые кнопки, ошибка) → проблема в лаунчере. См. часть II.
- **Окно лаунчера закрылось**, но клиент 1С не открывается (или открылся с ошибкой соединения с сервером) → проблема в сетевой части или в подключении к ИБ. См. части III и IV.
- **Появляется окно ввода логина/пароля**, но дальше ошибка → проблема в аутентификации. См. часть V.
- **Логин принят, но дальше ошибка про лицензию** → проблема в лицензировании. См. часть V.
- **Логин и лицензия в порядке, но появляется ошибка про конфигурацию или права** → проблема в подготовке прикладного решения. См. часть VI.
- **Появляется ошибка «в модуле сеанса» или «в модуле управляемого приложения»** → проблема в коде модулей. См. часть VII.
- **Главное окно начинает строиться, но падает с ошибкой** → проблема в обработчиках начала работы. См. часть VIII.

#### Шаг 2: типичные первые проверки

Если вы определили этап — переходите к соответствующей части статьи и читайте врезки с ошибками. Если этап не определён или хочется проверить «всё базовое сразу» — вот короткий чек-лист.

**Сетевая доступность сервера** (если база серверная):

```
ping srv-1c.company.local
nslookup srv-1c.company.local
Test-NetConnection -ComputerName srv-1c.company.local -Port 1540
```

Если хотя бы одна из этих команд не работает — проблема в сети, не в 1С. Идите к сетевому администратору.

**Доступность веб-публикации** (если база веб):

Откройте URL базы в обычном браузере (например, `https://1c.company.com/accounting/`). Если открывается стартовая страница или служебная информация — веб-сервер живой. Если нет — проблема на стороне веб-сервера или в сети до него.

**Состояние службы сервера 1С** (если у вас есть доступ на сам сервер):

В Windows откройте «Службы» (`services.msc`) и проверьте, что служба «Агент сервера 1С:Предприятия 8.x» запущена. Если нет — запустите.

**Список баз в лаунчере**:

Если в окне лаунчера пустой список — проверьте файл `%APPDATA%\1C\1CEStart\ibases.v8i`. Если он пустой или его нет — добавьте базу через кнопку «Добавить» или попросите системного администратора прислать `.v8i` файл.

**Кеш конфигурации**:

Если у вас странные ошибки при подключении к базе, которую раньше открывали без проблем, попробуйте удалить кеш конфигурации:

1. Закройте все окна 1С.
2. Удалите каталог `%APPDATA%\1C\1cv8\<hash>\` для проблемной базы (точный hash можно найти, посмотрев timestamp каталогов).
3. Альтернативно — удалите запись базы из лаунчера и добавьте её заново (это автоматически сменит GUID и создаст новый каталог кеша).

**Лицензии**:

Если ошибка про лицензию — проверьте:
- USB-ключ HASP физически воткнут в компьютер (в случае аппаратной лицензии)
- Программные лицензии не слетели после обновления железа (в случае программных)
- Сервер лицензирования отвечает по сети (в случае централизованного режима)

**Версия платформы**:

Если ошибка про несовместимость версий — проверьте `Version` в записи базы (`.v8i`) и фактически установленные версии в `C:\Program Files\1cv8\`. Если нужная версия отсутствует, и `DistributiveLocation` не настроен — нужно ставить версию вручную.

#### Шаг 3: куда смотреть в журналах

Если базовые проверки ничего не дали, начинаем смотреть в логи. Источники по приоритету:

1. **Журнал регистрации** в самой ИБ — откроется, только если в базу можно войти. Часто содержит подробности про ошибки последних сеансов.
2. **Логи кластера** в `srvinfo` (на хосте с сервером 1С) — содержат информацию про запуск и подключения процессов кластера.
3. **Технологический журнал** через `logcfg.xml` — самый детальный, но требует настройки и не для всех ситуаций нужен.
4. **Журнал событий Windows** — если проблема выглядит как ошибка ОС или службы.
5. **Логи СУБД** — если проблема похожа на ошибку базы данных.

#### Шаг 4: «классический ритуал»

Если ничего не помогает, есть «классический ритуал» одинэсника, который иногда помогает в непонятных ситуациях:

1. **Закрыть все клиенты** этой базы.
2. **Удалить кеш конфигурации** (`%APPDATA%\1C\1cv8\<hash>\` или пересоздание записи в лаунчере).
3. **Перезапустить службу сервера 1С** (если у вас есть доступ).
4. **Войти заново**.

Это не решение проблемы, а способ «сбросить все кеши» и проверить, не было ли проблемой что-то застрявшее. Если после этого работает — проблема была в кешах, и можно вернуться к обычной работе. Если не работает — переходите к серьёзной диагностике.

#### Если ничего не помогает

В этом случае — собираете информацию (тип ошибки, скриншоты, время, что делали перед ней, что в логах) и идёте либо к коллегам-одинэсникам, либо в техподдержку. Чем больше контекста — тем быстрее найдут причину.

И помните главный принцип: **сначала определите этап, потом ищите причину**. Лечить «1С не работает» вообще — невозможно. Лечить «у пользователя Иванова при подключении к базе X через тонкий клиент после ввода пароля появляется ошибка Y» — задача, у которой есть конкретный ответ.

---

## Финальное напутствие

Это была длинная статья. Если вы дочитали её до конца — спасибо за терпение. Я надеюсь, что после неё одно из самых обыденных действий в жизни одинэсника — двойной клик по ярлыку 1С — приобрело новый смысл. За простым «зашёл в базу и работаешь» стоит десяток механизмов, каждый из которых заслуживает отдельной статьи: лаунчер, сетевой стек, кластер, СУБД, аутентификация, лицензии, метаданные, расширения, права, RLS, компиляция, формы, рендеринг.

Каждый из этих механизмов — это инженерное решение, и у каждого есть своя история, свои сильные стороны и свои недостатки. 1С — большая и сложная платформа, в которой много вещей сделано необычно по сравнению с «обычной разработкой», но почти всегда — с понятной мотивацией. Понимание этой мотивации делает работу с 1С менее похожей на магию и более похожей на инженерию.

Если что-то осталось непонятным или хочется углубиться — приложение B со ссылками может помочь. Если нашли ошибку или у вас есть что добавить — это нормально, статья живая, её можно править.

И последнее. В начале статьи я писал, что цель — показать, что 1С не «выпендривается со своими странностями», а пользуется мировыми стандартами там, где это разумно, и имеет собственные форматы там, где специфика учётной системы этого требует. Я надеюсь, что после девяти частей с подробностями этот тезис стал для вас не просто словами, а сводом конкретных наблюдений. 1С — это нормальная инженерная платформа, со своими решениями и своими компромиссами, и в этом смысле она ничем не отличается от любой другой большой платформы, которой пользуются миллионы разработчиков.

Удачных вам запусков. И пусть ваши `rphost` не падают.
